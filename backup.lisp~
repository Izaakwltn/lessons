;;;;backup.lisp

(in-package :lessons)

;;;write to

(defun add-lesson (month day last first duration notes)
  (format nil "will figure out later"))

(defun duplicate-check (lesson))

(defun save-lesson (lesson)
  "write to backup.db")
